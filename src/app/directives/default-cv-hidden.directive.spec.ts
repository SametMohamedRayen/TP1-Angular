import { DefaultCvHiddenDirective } from './default-cv-hidden.directive';

describe('DefaultCvHiddenDirective', () => {
  it('should create an instance', () => {
    const directive = new DefaultCvHiddenDirective();
    expect(directive).toBeTruthy();
  });
});
